<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HomeActivity">

    <!-- Tombol Lihat Jadwal -->
    <Button
        android:id="@+id/btnLihatJadwal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Lihat Jadwal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="20dp" />

    <!-- Tombol Pesan Tiket -->
    <Button
        android:id="@+id/btnPesanTiket"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Pesan Tiket"
        app:layout_constraintTop_toBottomOf="@id/btnLihatJadwal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="20dp" />

    <!-- Tombol Riwayat Pemesanan -->
    <Button
        android:id="@+id/btnRiwayat"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Riwayat Pemesanan"
        app:layout_constraintTop_toBottomOf="@id/btnPesanTiket"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="10dp"
        android:layout_marginHorizontal="20dp" />

    <!-- Tombol Kelola Jadwal -->
    <Button
        android:id="@+id/btnKelola"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Kelola Jadwal"
        app:layout_constraintTop_toBottomOf="@id/btnRiwayat"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="10dp"
        android:layout_marginHorizontal="20dp" />

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:menu="@menu/bottom_nav_menu"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:ignore="MissingConstraints" />

</androidx.constraintlayout.widget.ConstraintLayout>
